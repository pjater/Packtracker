services:
  - type: web
    name: packtracker
    runtime: static
    staticPublishPath: .
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /(?!.*\\.\\w+$|.*\\/$)(.*)$
        destination: /index.html
